//We are introducing comments early in this tutorial series because,
//from now on, we will be using them to explain our code.
//This is single line comment

#include <stdio.h>

int main() {

                                            // create integer variable    
  int age = 22; 
 
                                             // print the age variable
  printf("Now Iam: %d years old.", age);

  return 0;
}

/* This program takes age input from the user
It stores it in the age variable
And, print the value using printf() */
//This is multiple line comment