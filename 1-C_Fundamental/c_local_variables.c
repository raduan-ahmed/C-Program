/*Variables that are declared inside the functions with the keyword local are called local variables.
The local variable's scope is inside the function in which it is declared. */

#include <stdio.h>
int main()
{
    int a =10;
    printf("local variable is : %d",a);
    return 0;
}